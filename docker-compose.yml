version: '3.8'

services:
  # Contenedor del Backend (Flask + Python)
  backend:
    build:
      context: ./fitcol-project-backend
      dockerfile: Dockerfile
    container_name: fitcol-backend
    ports:
      - "8000:8000"
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
      - DATABASE_URL=sqlite:///fitcol.db
    volumes:
      - ./backend:/app
      - backend-data:/app/data
    networks:
      - fitcol-network
    restart: unless-stopped

  # Contenedor del Frontend (Nginx + HTML/CSS/Bootstrap)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: fitcol-frontend
    ports:
      - "3000:3000"
    environment:
      - API_URL=http://backend:8000
    depends_on:
      - backend
    networks:
      - fitcol-network
    restart: unless-stopped

volumes:
  backend-data:

networks:
  fitcol-network:
    driver: bridge